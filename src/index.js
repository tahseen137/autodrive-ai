import React from 'react';
import ReactDOM from 'react-dom/client';
import CarBuyerWebsite from './CarBuyerWebsite';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CarBuyerWebsite />
  </React.StrictMode>
);
